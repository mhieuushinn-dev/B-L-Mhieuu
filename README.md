# ShinnH4K

Build preparation for iOS 17+.

## Build with GitHub Actions
1. Push the repository to GitHub.
2. Open **Actions**.
3. Run **Build ShinnH4K** using **Run workflow**.
4. Review the `build-log` artifact if compilation fails.

The project has been updated with the `IMG_0508.jpeg` image as the app avatar and app icon.
